{
  "name": "logoped-api",
  "buildCommand": "cd ../.. && corepack enable pnpm && pnpm install && pnpm --filter @workspace/api-server run build",
  "outputDirectory": "dist",
  "framework": "express"
}
