[build]
  builder = "nixpacks"
  buildCommand = "npm install -g pnpm@10.11.0 && pnpm install && pnpm --filter @workspace/api-server run build"

[deploy]
  startCommand = "node artifacts/api-server/dist/index.mjs"
  restartPolicyMaxRetries = 5

[env]
  PORT = "3000"
