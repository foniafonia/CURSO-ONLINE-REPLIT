[build]
  builder = "nixpacks"
  buildCommand = "corepack enable pnpm && pnpm install && pnpm run build"

[deploy]
  startCommand = "PORT=3000 node index.js"
  restartPolicyMaxRetries = 5

[env]
  PORT = "3000"
